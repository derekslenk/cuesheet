# Phase 3A (raw) — Test Strategy & Coverage Review

**Suite status (measured 2026-06-15):** `npx jest --coverage` → **77 suites, 609 tests, all passing, 9.0s**. Zero skipped/.only/.todo. Exit code 1 is ONLY the global coverage threshold (statements/lines 31.79% vs 70% configured). No flaky indicators.

**Headline:** The newest code (overlay/stream-label, twitch, overlayData, overlayMetrics, liveSeed, supervisor, streamdeck, loadDriver) is genuinely well tested — behavior-focused, error paths covered, security assertions present. Risk concentrated in the oldest, OBS-facing, security-critical core: `middleware.ts` (auth), `lib/security.ts` (input validation), `lib/obsClient.js` (1,400 lines), and the mutating API routes — all at/near 0% line coverage.

## Coverage gate reality
`coverageThreshold` (70% global, 80% supervisor) is NOT enforced — `.github/workflows/build.yml:84-87` runs `npm test` (plain jest) not `test:ci`, with a comment that the app's overall coverage doesn't meet 70% yet. Threshold is decorative for the global bucket; only the supervisor sub-tree (~88%) is effectively defended.

Per-area (coverage/coverage-summary.json): overlayData/overlayMetrics/twitch/liveSeed 100; app/api/overlay/** 100; StreamLabel.tsx 92.4; supervisor ~89; streamdeck/loadDriver/convertBrowserToMedia 81-100; database.ts 97.6; streams/[id] route 28.1; teams/[teamId] 41.6; **security.ts 0; middleware.ts absent/0; obsClient.js absent/0; addStream 0; setActive/setScene/triggerTransition/syncGroups/verifyGroups 0; all React pages + components 0.**

## CRITICAL
- **C1. middleware.ts auth completely untested.** Only auth layer, zero tests. Three fail-open behaviors that should be PINNED precisely because intentional-but-dangerous: unset API_KEY → next() (16-19); accepts key via ?apikey= query (12); bypasses auth for Host starting localhost/127.0.0.1/192.168 (22-25, client-controlled/spoofable). middleware() is a pure function of NextRequest — cheap to test. Highest-value missing test. (Provided full middleware.test.ts example with @jest-environment node, covering fail-open, 401, header+query key, spoofable-Host pinned as RED FLAG, OPTIONS preflight.)
- **C2. lib/security.ts 0% coverage.** validateStreamInput (addStream:59), validateScreenInput (setActive), validateInteger (createGroup), isValidUrl/isPositiveInteger/sanitizeString — front-line guards for every mutating write, zero assertions. Pure functions. Untested edges: isValidUrl rejecting javascript:/file:/data: (only http(s), security.ts:11-18); sanitizeString stripping <>"/\& + length cap (33-36); validateStreamInput rejecting string team_id. (Provided security.test.ts example.)
- **C3. lib/obsClient.js ~1,400 lines untested + obsClient.test.js is MISNAMED** (it tests streamInputConfig.js, never obsClient.js; jest can't import ESM-only obs-websocket-js at module top — the documented reason DI helpers were extracted). Untested load-bearing: ensureConnected/connectToOBS singleton race guard (33-96); createStreamGroupV2 both LABEL_RENDERER branches (783-1047); deleteStreamComponents/deleteTeamComponents OBS-crash-avoidance ordering + OBS_BULK_PACE_MS (1059-1378); measureTextWidth two-equal-reads (764-777). Fix: finish the DI refactor (extract orchestration taking {call}), test the call sequence vs jest.fn mock. Highest-value first test: assert deletion detaches from switchers BEFORE RemoveScene.

## HIGH
- **H1. PUT /api/streams/[id] validation gap real AND untested (S-F3).** PUT validates presence only (route.ts:47), never isValidUrl/team_id-int (unlike addStream→validateStreamInput). url:'javascript:alert(1)' or team_id:'abc' persisted verbatim (70-75). streamsRole.test.ts exercises PUT only with valid bodies; DELETE + 404/500 uncovered (28% lines). Fix route first, then pin: js-url→400+no db.run, non-int team_id→400, 404 path, DELETE 404 + OBS-cleanup-fails-but-db-delete-succeeds branch (144-158).
- **H2. Two-writer SQLite PRAGMA parity unpinned (A-C1/S-F2).** database.wal.test.ts covers only Node opener, loosely (asserts busy_timeout>=1; Node sets 5000). bun opener sets WAL+busy_timeout 5000 + wal_autocheckpoint 1000 (extra), bun-only so never executed in CI. "Supervisor fails loud when table absent" has no test. Fix: tighten Node test to exact 5000 + journal_mode=wal; extract PRAGMA list to shared constant consumed by both + AST/string guard test (like tableNameHardcodeGuard); add table-absent-rejects-loudly test at streamSpecsLoader/MinimalDb DI seam.
- **H3. getViewerCounts batching + offline-null caching untested (P-C1/P-C2).** twitch.ts 100% line/80% branch, single-batch cache test good, but: the i+=100 batching loop (175) never exercised with >100 logins; the offline-null caching branch (186-188, caches {count:null} so not re-queried) never asserted to skip re-request next poll — the exact anti-hammer mechanism. Provided tests: >100 logins → 2 /streams requests; offline login cached null + not re-queried within TTL.
- **H4. No test for addStream or any mutating OBS route** (addStream/setActive/setScene/triggerTransition/syncGroups/verifyGroups all 0%) — operator's primary live-event actions. addStream most consequential: insert + createStreamGroupV2 + relay port + rollback path (53). Mock @/lib/obsClient + @/lib/database (pattern proven in streamsRole.test.ts). Prioritize: invalid→400+no insert; happy path→insert + createStreamGroupV2 args; OBS-throws-after-insert→rollback deletes row.

## MEDIUM
- **M1. db-init race (P-H1) unpinned** (database.ts:102-124 check-then-await-open-then-assign). Jest single-threaded won't reproduce incidentally but can pin intended memoization (Promise.all([getDatabase(),getDatabase()]) → same handle). If fails, race is live; fix = memoize in-flight promise (like obsClient.ensureConnected).
- **M2. No e2e/browser tests** (no Playwright/Cypress/vitest config). All React pages 0% (streams 806 / teams 706 / settings 319 / edit 366); overlay page.tsx 0% (only inner StreamLabel.tsx tested in jsdom). Partially fine (control-plane logic tested at unit/route layer) BUT the OBS overlay is where "renders correctly in a browser" IS the product requirement — jsdom has no layout/paint, so a CSS/font/transparent-bg regression ships green. Recommend ONE Playwright smoke: load /overlay/stream/[id] with mocked API, assert team/name/viewer text visible + background transparent (repo already has playwright-core + chromium per README-screenshot memory).
- **M3. Load/soak harnesses are scripts not assertions.** loadDriver/atomicWriteSoak pure pieces ~100% covered, but harnesses themselves 0% (run by hand). Defensible split. Genuine gap: viewer-polling fan-out under many labels has no automated load assertion (M labels → ≤⌈M/100⌉ Helix calls per TTL window). Overlaps H3.

## LOW / fine
- L1 supervisorClient 57% / teams/[teamId] 42% — critical paths (start/stop/restart) 100%, acceptable.
- L2 performance.ts/apiClient.ts/several components 0% — non-event-critical, deferring reasonable.
- L3 Mock hygiene good (inject {call}, mock getDatabase/fetch, restore env afterEach, resetModules for singletons, real-fs test cleans up). Low flake risk.
- L4 Test quality high where tests exist — fresh-code tests assert behavior+security (overlayData CSS-injection/path-traversal rejection; overlayViewers graceful degradation + no-store + malformed-id-short-circuit; StreamLabel 404 vs 5xx). Gaps are absence, not weakness.

## Priority order
1. C1 middleware.test.ts (auth) ~1hr pure fn. 2. C2 security.test.ts pure fns. 3. H1 streams/[id] PUT (fix route + test). 4. H2 pin two-writer PRAGMA parity. 5. C3/H4 extract+test obsClient orchestration (deletion order) + addStream happy/rollback. 6. H3/M3 Twitch batching/offline-null/fan-out. 7. M2 one Playwright overlay smoke.
